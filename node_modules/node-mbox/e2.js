const { simpleParser } = require('mailparser');
const Mbox             = require('.');
const mbox             = new Mbox({ _stream : true });

mbox.on('message', function(msg) {
  console.log('new message');
//  mbox.pause();
}).on('error', e => {
  console.log('E', e);
}).on('end', _ => {
  console.log('end');
}).on('finish', _ => {
  console.log('finish');
});

process.stdin.pipe(mbox);
